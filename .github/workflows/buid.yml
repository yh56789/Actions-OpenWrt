name: Build ImmortalWrt 24.10.5 x86_generic

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出仓库代码（获取你的 .config 和 config-6.6）
        uses: actions/checkout@v4

      - name: 查看服务器信息
        run: |
          echo "CPU 核心数: $(nproc)"
          free -h
          df -h

      - name: 安装编译依赖
        run: |
          sudo apt update -y
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
          git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev \
          libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
          mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip libpython3-dev qemu-utils \
          rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev

      - name: 克隆 ImmortalWrt 源码（固定 v24.10.5 标签）
        run: |
          git clone -b v24.10.5 --single-branch --filter=blob:none https://github.com/immortalwrt/immortalwrt.git immortalwrt

      # ==================== 关键：应用你自己的配置文件 ====================
      - name: 应用你自己的固件配置 .config
        run: |
          cp .config immortalwrt/.config

      - name: 应用你自己的 x86 内核配置 config-6.6
        run: |
          cp config-6.6 immortalwrt/target/linux/x86/generic/config-6.6

      # =================================================================
      - name: 更新 feeds
        run: |
          cd immortalwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 加载配置
        run: |
          cd immortalwrt
          make defconfig

      - name: 下载依赖
        run: |
          cd immortalwrt
          make download -j$(nproc)

      - name: 开始编译固件
        run: |
          cd immortalwrt
          make -j$(nproc) V=s

      - name: 上传编译好的固件
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-24.10.5-x86_64
          path: immortalwrt/bin/targets/
